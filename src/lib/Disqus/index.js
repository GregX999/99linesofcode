import { CommentCount } from "./CommentCount";
import { ThreadEmbed } from "./ThreadEmbed";

export { CommentCount };
export { ThreadEmbed };

const Disqus = {
  CommentCount,
  ThreadEmbed
};

export default Disqus;
